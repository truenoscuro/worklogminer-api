@Library("re-library") _
// posar se config 

cfg.isWindows()

node{
    echo "es re-estrucutracio"
    td.init this , env
    
    // extrac de functio cmd escriu en bat si es windows o en sh si es linux    
    def cmd = td.utils.cmd

    
}